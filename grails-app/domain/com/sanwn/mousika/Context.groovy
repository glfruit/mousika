package com.sanwn.mousika

/**
 * 代表一个用户赋予的某个角色成立的范围，即在某个范围内用户具有该角色
 * 例如一个用户在某门课程为教师，而在另一门课程为学生，则第一个角色“教师”
 * 的范围为该课程，第二个角色“学生”的范围为另一门课程
 *
 * @author glix
 * @version 1.0
 *
 */
class Context {

    int level
}
